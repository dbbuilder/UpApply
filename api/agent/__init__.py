"""UpApply autonomous agent layer.

Modules:
  rss_adapter     — fetch jobs from Upwork RSS feed
  job_discovery   — JobDiscoveryAgent (Anthropic tool-use loop)
  run_discovery   — Render cron entry point
"""
